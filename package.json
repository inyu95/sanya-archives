{
  "name": "cesium-project",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "npx --yes http-server -p 8080 -c-1",
    "dev": "npx --yes live-server --port=8080 --no-browser --ignore=node_modules --ignore=.git",
    "sync-seo": "node scripts/sync-seo.cjs",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "dependencies": {
    "cesium": "^1.142.0"
  }
}

{
  "name": "cesium-project",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "photos:index": "node scripts/generate-photos-index.js",
    "prestart": "npm run photos:index",
    "start": "npx --yes http-server -p 8080 -c-1",
    "predev": "npm run photos:index",
    "dev": "npx --yes live-server --port=8080 --no-browser",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "dependencies": {
    "cesium": "^1.142.0"
  }
}
